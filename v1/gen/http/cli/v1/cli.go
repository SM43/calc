// Code generated by goa v3.2.6, DO NOT EDIT.
//
// v1 HTTP client CLI support package
//
// Command:
// $ goa gen github.com/sm43/calc/v1/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	calcc "github.com/sm43/calc/v1/gen/http/calc/client"
	statusc "github.com/sm43/calc/v1/gen/http/status/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//    command (subcommand1|subcommand2|...)
//
func UsageCommands() string {
	return `status status
calc v1-add
`
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` status status` + "\n" +
		os.Args[0] + ` calc v1-add --a 7059804407227146546 --b 5605287781138967563` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, interface{}, error) {
	var (
		statusFlags = flag.NewFlagSet("status", flag.ContinueOnError)

		statusStatusFlags = flag.NewFlagSet("status", flag.ExitOnError)

		calcFlags = flag.NewFlagSet("calc", flag.ContinueOnError)

		calcV1AddFlags = flag.NewFlagSet("v1-add", flag.ExitOnError)
		calcV1AddAFlag = calcV1AddFlags.String("a", "REQUIRED", "Left operand")
		calcV1AddBFlag = calcV1AddFlags.String("b", "REQUIRED", "Right operand")
	)
	statusFlags.Usage = statusUsage
	statusStatusFlags.Usage = statusStatusUsage

	calcFlags.Usage = calcUsage
	calcV1AddFlags.Usage = calcV1AddUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "status":
			svcf = statusFlags
		case "calc":
			svcf = calcFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "status":
			switch epn {
			case "status":
				epf = statusStatusFlags

			}

		case "calc":
			switch epn {
			case "v1-add":
				epf = calcV1AddFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     interface{}
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "status":
			c := statusc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "status":
				endpoint = c.Status()
				data = nil
			}
		case "calc":
			c := calcc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "v1-add":
				endpoint = c.V1Add()
				data, err = calcc.BuildV1AddPayload(*calcV1AddAFlag, *calcV1AddBFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// statusUsage displays the usage of the status command and its subcommands.
func statusUsage() {
	fmt.Fprintf(os.Stderr, `The status service provides status of the service.
Usage:
    %s [globalflags] status COMMAND [flags]

COMMAND:
    status: Status implements status.

Additional help:
    %s status COMMAND --help
`, os.Args[0], os.Args[0])
}
func statusStatusUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] status status

Status implements status.

Example:
    `+os.Args[0]+` status status
`, os.Args[0])
}

// calcUsage displays the usage of the calc command and its subcommands.
func calcUsage() {
	fmt.Fprintf(os.Stderr, `The calc service performs operations on numbers.
Usage:
    %s [globalflags] calc COMMAND [flags]

COMMAND:
    v1-add: V1Add implements v1Add.

Additional help:
    %s calc COMMAND --help
`, os.Args[0], os.Args[0])
}
func calcV1AddUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] calc v1-add -a INT -b INT

V1Add implements v1Add.
    -a INT: Left operand
    -b INT: Right operand

Example:
    `+os.Args[0]+` calc v1-add --a 7059804407227146546 --b 5605287781138967563
`, os.Args[0])
}
